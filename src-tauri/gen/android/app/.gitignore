# 注意：tauri CLI 生成的 Kotlin 源码（`src/main/**/generated/`：WryActivity、
# RustWebView、TauriActivity 等）**故意不忽略、已随仓库分发** —— CI 在全新克隆里编译，
# 缺这些文件会直接报 `MainActivity.kt: Unresolved reference: TauriActivity`。
# 同理 `proguard-tauri.pro`（R8 保留规则）与 `tauri.properties`（版本号）也必须入库。
# 只有 `tauri.build.gradle.kts` 不入库：它含本机绝对路径，由 CI 现生成。
# 原来这里是 `/src/main/jniLibs/**/*.so`（Tauri 模板写的）。
# 模板假设 jniLibs 里只有 Rust 编出来的库，但本项目的 jniLibs 同时存放
# 上游 Pojav 的**预编译资产**：liblwjgl*.so / libopenal.so / libOSMesa.so /
# libgl4es_114.so / libvulkan_freedreno.so / libfreetype.so / libjnidispatch.so /
# libunpack200.so —— 它们是构建与运行的必需输入，仓库里没有别处能再生成，
# 一刀切会让 clone 出来的仓库根本编不出能跑的包。
# 这里只排除我们自己编的那个：
/src/main/jniLibs/**/libqookix_lib.so
/src/main/assets/tauri.conf.json
/tauri.build.gradle.kts